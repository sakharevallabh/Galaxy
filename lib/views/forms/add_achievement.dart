import 'package:flutter/material.dart';

class AddAchievementView extends StatelessWidget {
  const AddAchievementView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Padding(
        padding: EdgeInsets.all(10),
      ),
    );
  }
}